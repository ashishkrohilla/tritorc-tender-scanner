AI_NOTES.txt
============

AI tool used: Claude (Anthropic)

I was new to building a full-stack app under a tight deadline, so I used
Claude to help me plan the approach and scaffold the project structure.

Prompt 1:
"I have a take-home assignment to build a full-stack tender/SOW keyword
scanner (React + Node/Express). Here is the brief [pasted]. Help me
understand exactly what's required and scaffold the backend: file upload,
PDF/DOCX text extraction with pdf-parse/mammoth, keyword matching against a
config file, and Excel report generation with exceljs."
-> Used as a starting scaffold for server.js, routes/scan.js, and
   utils/extractText.js.

Prompt 2:
"Write the keyword matching logic so it's case-insensitive and handles
simple variants like 'torque wrench' vs 'torque wrenches' without a full
stemming library, and make the relevance verdict computed dynamically from
match count rather than hardcoded."
-> Used directly for utils/keywordMatch.js, then reviewed line by line to
   understand the regex logic so I could explain it if asked.

Prompt 3:
"Build a simple responsive React upload UI (drag-and-drop, file list,
results table, Excel download) and then give it a more polished, industrial
themed look instead of a generic template style."
-> Used as a base for App.jsx / UploadForm.jsx / ResultsTable.jsx / index.css.

I then set everything up on my own machine (installed Node.js, VS Code, ran
the backend and frontend servers, tested with real sample tender PDFs), read
through the code to understand how each part works, and pushed it to GitHub
myself. I take responsibility for understanding and being able to explain
every part of this codebase.
